{
  "name": "@coops/sse",
  "private": true,
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@types/koa": "^2.13.1",
    "@types/koa__cors": "^3.0.2",
    "esbuild": "^0.11.23"
  },
  "scripts": {
    "dev": "esbuild src/index.ts --platform=node --target=node16 --format=cjs --sourcemap --outdir=dist",
    "build": "esbuild src/index.ts --platform=node --target=node16 --format=cjs --minify --outdir=dist"
  },
  "dependencies": {
    "@koa/cors": "^3.1.0",
    "koa": "^2.13.1"
  }
}
